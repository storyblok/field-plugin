{
  "extends": "../../tsconfig.base.json",
  // vite-plugin-dts will output at the root level unless specified here
  "compilerOptions": {
    "outDir": "dist"
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts"
  ]
}
