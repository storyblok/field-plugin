{
  "name": "@storyblok/field-plugin-cli",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "field-plugin": "./bin.js"
  },
  "files": [
    "bin.js",
    "dist",
    "templates"
  ],
  "scripts": {
    "prepare": "vite build",
    "generate": "yarn build:quiet && node ./dist/main.cjs generate",
    "deploy": "yarn build:quiet && node ./dist/main.cjs deploy",
    "build": "vite build",
    "build:quiet": "vite build --logLevel error",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "commander": "^9.5.0",
    "dotenv": "^16.0.3",
    "execa": "^6.1.0",
    "kleur": "^4.1.5",
    "mustache": "^4.2.0",
    "node-fetch": "^3.3.0",
    "prompts": "^2.4.2",
    "vite": "^4.0.3",
    "walkdir": "^0.4.1"
  },
  "devDependencies": {
    "@types/execa": "2.0.0",
    "@types/mustache": "4.2.2",
    "@types/prompts": "2.4.2",
    "vitest": "0.25.3"
  }
}
